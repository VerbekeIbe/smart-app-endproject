import { StyleSheet } from "react-native";

export const loading = StyleSheet.create({
    neutral: {
        flex:1,
        flexDirection:'row',
        alignItems:'center',
        justifyContent:'center'
    }
});